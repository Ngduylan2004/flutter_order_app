import 'package:flutter/material.dart';

class Sdfd extends StatelessWidget {
  const Sdfd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sdfd'),
      ),
      body: const Center(child: Text('Sdfd')),
    );
  }
}
